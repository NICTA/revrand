language: python

sudo: false

python:
  - "2.7"
  - "3.4"

virtualenv:
  system_site_packages: true

addons:
  apt:
    packages:
    - libblas-dev
    - liblapack-dev
    - libatlas-base-dev
    - gfortran
    - python-nlopt

before_install:
  - pip install --upgrade pip
  - pip install unipath requests 

install: 
  - python setup.py install

script: py.test

cache:
  - apt
  - pip
  - directories:
    - $HOME/.cache/pip
